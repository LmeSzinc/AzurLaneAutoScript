# --------------------
# Define argument group of tasks.
# --------------------

# ==================== Alas ====================

Alas:
  menu: 'list'
  page: 'setting'
  tasks:
    Alas:
      - Emulator
      - EmulatorInfo
      - Error
      - Optimization
      - CloudStorage
    Restart:
      - Scheduler

# ==================== Daily ====================

Daily:
  menu: 'list'
  page: 'setting'
  tasks:
    Dungeon:
      - Scheduler
      - Dungeon
      - DungeonDaily
      - DungeonSupport
      - DungeonStorage
    DailyQuest:
      - Scheduler
      - AchievableQuest
      - DailyStorage
    BattlePass:
      - Scheduler
      - BattlePassStorage
    Assignment:
      - Scheduler
      - Assignment
    DataUpdate:
      - Scheduler
      - ItemStorage
    Freebies:
      - Scheduler
      - SupportReward

# ==================== Weekly ====================

Weekly:
  menu: 'list'
  page: 'setting'
  tasks:
    Weekly:
      - Scheduler
      - Weekly
      - DungeonSupport
    Rogue:
      - Scheduler
      - RogueWorld
      - RogueBlessing
      - RogueDebug

# ==================== Tool ====================

Tool:
  menu: 'list'
  page: 'tool'
  tasks:
    Daemon:
      - Daemon
